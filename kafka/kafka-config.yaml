apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: his-kafka
data:
  server.properties: |
    # KRaft mode configuration
    process.roles=broker,controller
    node.id=1
    controller.quorum.voters=1@his-kafka-0.his-kafka-headless.his-kafka.svc.cluster.local:9093
    
    # Listeners
    listeners=PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094
    advertised.listeners=PLAINTEXT://his-kafka-0.his-kafka-headless.his-kafka.svc.cluster.local:9092,EXTERNAL://NODE_IP:30092
    listener.security.protocol.map=PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
    controller.listener.names=CONTROLLER
    inter.broker.listener.name=PLAINTEXT
    
    # Cluster settings
    cluster.id=his-kafka-cluster-id
    
    # Log directories
    log.dirs=/var/lib/kafka/data
    
    # Replication settings
    offsets.topic.replication.factor=1
    transaction.state.log.replication.factor=1
    transaction.state.log.min.isr=1
    default.replication.factor=1
    min.insync.replicas=1
    
    # Topic settings
    num.partitions=1
    auto.create.topics.enable=false
    
    # Compression and retention
    compression.type=gzip
    log.retention.hours=168
    log.retention.bytes=1073741824
    log.segment.bytes=536870912
