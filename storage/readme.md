# Storage

This directory contains storage-related configurations.

## Structure
- `pv/`: PersistentVolume definitions (mapping to host storage).

- `*-storageclass.yaml`: Custom StorageClass definitions.

## Files
- `kafka-storageclass.yaml`: Defines the `kafka-local-storage` class with `Retain` policy.
