# NodePort Services

NodePort services expose applications on a static port on each node's IP, allowing external access to the cluster.

## Files
- `kafka-nodeport.yaml`: Exposes the Kafka brokers externally on port `30092`.
