# ConfigMaps

This directory contains `ConfigMap` YAML files which store non-confidential data in key-value pairs.

## Files
- `kafka-configmap.yaml`: Contains the `server.properties` configuration for the Kafka brokers.
